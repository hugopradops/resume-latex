# Archieve
Archieve/

# Tailored resumes for specific applications (kept locally, not versioned)
tailored-resumes/

# Job search notes and leads (kept locally, not versioned)
job-search/

## Core latex/pdflatex auxiliary files:
*.aux
*.lof
*.log
*.lot
*.fls
*.out
*.toc
*.fmt
*.fot
*.cb
*.cb2
.*.lb

# github action result
semicolon_delimited_script